{% extends 'base.admin.html.twig' %}

{% block title %}Edit Page{% endblock %}

{% block body %}
<div class="col-12 p-5 rounded bg-light shadow">
    <div class="row">
        <h1 class="col h3">{{ page.title }}</h1>
        {{ include('administration/page/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <a href="{{ path('page_index') }}">back to list</a>
        {% if is_granted('ROLE_ADMIN') %}
            {{ include('administration/page/_delete_form.html.twig') }}
        {% endif %}
    </div>
    {% endblock %}


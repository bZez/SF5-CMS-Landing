{{ form_start(form) }}<button class="btn bg-custom text-white  mb-3 col" type="submit">{{ button_label|default('Save') }}</button>
</div>
<hr>
<button id="color-picker-3" name="custom" class="w-100 btn bg-custom text-white" onclick="return false;">Changer la couleur principale</button>
<hr>
<div class="form-row">
    <div class="col-6">{{ form_row(form.nom) }}</div>
    <div class="col-6">{{ form_row(form.title) }}</div>
</div>


{{ form_row(form.description) }}
{{ form_row(form.keywords) }}
<div class="form-row">
    <div class="col-6">
        {{ form_row(form.logo) }}
    </div>
    <div class="col-6">
        {{ form_row(form.favicon) }}
    </div>
</div>
<div class="form-row">
    <div class="col-6 text-center">
        {{ form_row(form.isIndexed) }}
    </div>
    <div class="col-6 text-center">
        {{ form_row(form.isFollowed) }}
    </div>
</div>
{{ form_row(form.content) }}
{{ form_end(form) }}

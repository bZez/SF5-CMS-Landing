{% extends 'base.admin.html.twig' %}

{% block title %}Tag{% endblock %}

{% block body %}
<div class="col-12 p-5 rounded bg-light shadow">
    <h1>Tag</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ tag.position }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ tag.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tag_index') }}">back to list</a>

    <a href="{{ path('tag_edit', {'id': tag.id}) }}">edit</a>

    {{ include('administration/administration/tag/_delete_form.html.twig') }}
</div>
{% endblock %}
